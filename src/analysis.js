const { getTrips, getDriver } = require("api");

/**
 * This function should return the trip data analysis
 *
 * Trips Analysis
 *
 * @returns {any} Trip data analysis
 */

const analysis = async () => {};

analysis(); // do not remove this

module.exports = analysis;
